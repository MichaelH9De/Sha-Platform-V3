import { PageHeader } from "@/components/PageHeader";
import { MetricCard } from "@/components/MetricCard";
import { actions, deliverables, fees, projects, risks } from "@/lib/demo-data";
export default function Page() { const red = projects.filter((p) => p.health === "RED").length; const overdue = actions.filter((a) => a.status === "OVERDUE").length; const blocked = deliverables.filter((d) => d.status === "BLOCKED").length; const exposure = fees.filter((f) => f.variance < 0).reduce((sum, f) => sum + Math.abs(f.variance), 0); return <section className="stack"><PageHeader eyebrow="Fictional live prototype" title="Enterprise Control Dashboard">A populated command centre showing the intended consultancy operating model without accounts or setup.</PageHeader><div className="warning-panel"><strong>Demo mode:</strong> all organisations, people, projects and figures are fictional.</div><div className="grid cols-4"><MetricCard label="Live projects" value={String(projects.length)} /><MetricCard label="Red projects" value={String(red)} /><MetricCard label="Overdue actions" value={String(overdue)} /><MetricCard label="Blocked deliverables" value={String(blocked)} /></div><div className="grid cols-2"><div className="panel"><h2>Portfolio health</h2><ul className="list">{projects.map((p) => <li key={p.id}><strong>{p.name} · {p.health}</strong><br /><small>{p.client} · {p.stage} · {p.manager}</small></li>)}</ul></div><div className="panel"><h2>Control focus</h2><p>{risks.length} active risks and £{exposure.toLocaleString("en-GB")} forecast fee exposure require review.</p></div></div></section>; }
